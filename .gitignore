.DS_Store
__pycache__/
*.py[cod]

agent_lab/logs/*.log
agent_lab/models/
agent_lab/workspace/extracted_tmp_archive_*
agent_lab/esphome/
tests/

# Verba source now lives in the verba store; keep local verba files out of this repo.
verba/*
!verba/__init__.py

# Portal source now lives in the portal store; keep local portal files out of this repo.
portals/*
!portals/__init__.py

# Core source now lives in the core store; keep local core files out of this repo.
cores/*
!cores/__init__.py

# Local runtime config/cache.
.runtime/
