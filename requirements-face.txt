# Optional local Face ID model pack. Installed when Face ID is enabled.
deepface==0.0.100
# Apple's Metal plug-in currently supports the TensorFlow 2.18 line.
tf-keras==2.18.0 ; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow==2.18.0 ; sys_platform == "darwin" and platform_machine == "arm64"
# Linux CUDA, Linux ARM CPU, and other CPU platforms use the current TensorFlow line.
tf-keras==2.21.0 ; sys_platform != "darwin" or platform_machine != "arm64"
tensorflow==2.21.0 ; sys_platform != "darwin" or platform_machine != "arm64"
# Apple Metal or NVIDIA CUDA extras are added by face_id_runtime when supported hardware is visible.
# DeepFace's OpenCV detector expects the bundled Haar cascade data.
opencv-python==4.11.0.86
